import {createClient} from '@/lib/supabase/server';
export default async function Page(){const s=await createClient();const{data:rows}=await s.from('usuarios').select('*').order('nome');return <section><h1 className="page-title">Usuários</h1><p className="muted">Acessos e perfis do sistema.</p><div className="card table-card"><div className="table-head"><h2 className="section-title" style={{margin:0}}>Acessos cadastrados</h2><span className="muted">{rows?.length||0} usuário(s)</span></div><div className="table-wrap"><table><thead><tr><th>Nome</th><th>E-mail</th><th>Perfil</th><th>Status</th></tr></thead><tbody>{(rows||[]).map((r:any)=><tr key={r.id}><td><b>{r.nome||'Sem nome'}</b></td><td>{r.email||'—'}</td><td><span className="badge blue">{r.perfil}</span></td><td><span className={'badge '+(r.ativo?'green':'gray')}>{r.ativo?'Ativo':'Inativo'}</span></td></tr>)}</tbody></table>{!rows?.length&&<div className="empty">Nenhum usuário cadastrado.</div>}</div></div></section>}