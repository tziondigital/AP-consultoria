import {createClient} from '@/lib/supabase/server';import {revalidatePath} from 'next/cache';import {EditModal,DeleteButton} from '@/components/RecordActions';import {label} from '@/lib/format';
export default async function Page(){
async function criar(f:FormData){'use server';const s=await createClient();await s.from('candidatos').insert({nome:String(f.get('nome')),email:String(f.get('email')||'')||null,telefone:String(f.get('telefone')||'')||null,cidade:String(f.get('cidade')||'')||null,estado:String(f.get('estado')||'').toUpperCase()||null,cargo:String(f.get('cargo')||'')||null,situacao:String(f.get('situacao')||'disponivel')});revalidatePath('/candidatos')}
async function editar(f:FormData){'use server';const s=await createClient();await s.from('candidatos').update({nome:String(f.get('nome')),email:String(f.get('email')||'')||null,telefone:String(f.get('telefone')||'')||null,cidade:String(f.get('cidade')||'')||null,estado:String(f.get('estado')||'').toUpperCase()||null,cargo:String(f.get('cargo')||'')||null,situacao:String(f.get('situacao')||'disponivel'),linkedin_url:String(f.get('linkedin_url')||'')||null,observacoes:String(f.get('observacoes')||'')||null}).eq('id',String(f.get('id')));revalidatePath('/candidatos')}
async function excluir(f:FormData){'use server';const s=await createClient();await s.from('candidatos').delete().eq('id',String(f.get('id')));revalidatePath('/candidatos')}
const s=await createClient();const{data:rows}=await s.from('candidatos').select('*').order('created_at',{ascending:false});
return <section><h1 className="page-title">Candidatos</h1><p className="muted">Banco de talentos e dados de contato.</p><details className="card form-card vacancy-details"><summary>+ Novo candidato</summary><form action={criar} className="quick-form"><div className="field-group"><label>Nome</label><input name="nome" required/></div><div className="field-group"><label>E-mail</label><input name="email" type="email"/></div><div className="field-group"><label>Telefone</label><input name="telefone"/></div><div className="field-group"><label>Cargo</label><input name="cargo"/></div><div className="field-group"><label>Cidade</label><input name="cidade"/></div><div className="field-group"><label>UF</label><input name="estado" maxLength={2}/></div><div className="field-group"><label>Situação</label><select name="situacao"><option value="disponivel">Disponível</option><option value="empregado">Empregado</option></select></div><button className="button">Cadastrar</button></form></details><div className="card table-card"><div className="table-head"><h2 className="section-title" style={{margin:0}}>Banco de talentos</h2><span className="muted">{rows?.length||0} candidato(s)</span></div><div className="table-wrap"><table><thead><tr><th>Nome</th><th>Cargo</th><th>E-mail</th><th>Telefone</th><th>Cidade/UF</th><th>Situação</th><th>Ações</th></tr></thead><tbody>{(rows||[]).map((r:any)=><tr key={r.id}><td><b>{r.nome}</b></td><td>{r.cargo||'—'}</td><td>{r.email||'—'}</td><td>{r.telefone||'—'}</td><td>{[r.cidade,r.estado].filter(Boolean).join(' / ')||'—'}</td><td><span className="badge blue">{label(r.situacao||'disponivel')}</span></td><td><div className="actions-cell"><EditModal title={'Editar candidato — '+r.nome} action={editar}><input type="hidden" name="id" value={r.id}/><div className="field-group"><label>Nome</label><input name="nome" required defaultValue={r.nome}/></div><div className="field-group"><label>Cargo</label><input name="cargo" defaultValue={r.cargo||''}/></div><div className="field-group"><label>E-mail</label><input name="email" type="email" defaultValue={r.email||''}/></div><div className="field-group"><label>Telefone</label><input name="telefone" defaultValue={r.telefone||''}/></div><div className="field-group"><label>Cidade</label><input name="cidade" defaultValue={r.cidade||''}/></div><div className="field-group"><label>UF</label><input name="estado" maxLength={2} defaultValue={r.estado||''}/></div><div className="field-group"><label>Situação</label><select name="situacao" defaultValue={r.situacao||'disponivel'}><option value="disponivel">Disponível</option><option value="empregado">Empregado</option></select></div><div className="field-group"><label>LinkedIn</label><input name="linkedin_url" defaultValue={r.linkedin_url||''}/></div><div className="field-group wide"><label>Observações</label><textarea name="observacoes" defaultValue={r.observacoes||''}/></div></EditModal><DeleteButton id={r.id} action={excluir} label="candidato"/></div></td></tr>)}</tbody></table>{!rows?.length&&<div className="empty">Nenhum candidato cadastrado.</div>}</div></div></section>}
