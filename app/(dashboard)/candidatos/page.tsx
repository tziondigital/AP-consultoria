import {createClient} from '@/lib/supabase/server';
export default async function Page(){const s=await createClient();const{data:rows}=await s.from('candidatos').select('*').order('created_at',{ascending:false});return <section><h1 className="page-title">Candidatos</h1><p className="muted">Banco de talentos e dados de contato.</p><div className="card table-card"><div className="table-head"><h2 className="section-title" style={{margin:0}}>Banco de talentos</h2><span className="muted">{rows?.length||0} candidato(s)</span></div><div className="table-wrap"><table><thead><tr><th>Nome</th><th>E-mail</th><th>Telefone</th><th>Cidade/UF</th><th>Currículo</th></tr></thead><tbody>{(rows||[]).map((r:any)=><tr key={r.id}><td><b>{r.nome}</b></td><td>{r.email||'—'}</td><td>{r.telefone||'—'}</td><td>{[r.cidade,r.estado].filter(Boolean).join(' / ')||'—'}</td><td>{r.curriculo_path?'Anexado':'—'}</td></tr>)}</tbody></table>{!rows?.length&&<div className="empty">Nenhum candidato cadastrado.</div>}</div></div></section>}