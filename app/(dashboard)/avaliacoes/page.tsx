import {createClient} from '@/lib/supabase/server';import {revalidatePath} from 'next/cache';
const comps=['Comunicação','Organização','Proatividade','Trabalho em equipe','Conhecimento técnico','Liderança'];
export default async function Page(){async function salvar(f:FormData){'use server';const s=await createClient();const{data:{user}}=await s.auth.getUser();const decisao=String(f.get('decisao')||'pendente');await s.from('avaliacoes').insert({candidatura_id:String(f.get('candidatura_id')),operador_id:user?.id||null,nota_tecnica:Number(f.get('nota_tecnica')),competencias:f.getAll('competencias').map(String),parecer:String(f.get('parecer')||'')||null,decisao});if(decisao!=='pendente')await s.from('candidaturas').update({status:decisao==='aprovar'?'aprovado':'reprovado'}).eq('id',String(f.get('candidatura_id')));revalidatePath('/avaliacoes')}const s=await createClient();const[{data:candidaturas},{data:rows}]=await Promise.all([s.from('candidaturas').select('id,status,candidatos(nome),vagas(cargo,empresas(nome))').not('status','in','("contratado","desistiu")'),s.from('avaliacoes').select('*,candidaturas(candidatos(nome),vagas(cargo)),usuarios(nome)').order('created_at',{ascending:false})]);return <section><h1 className="page-title">Avaliação de Candidatos</h1><p className="muted">Registre critérios técnicos, competências e parecer por processo.</p><div className="card form-card"><form action={salvar} className="evaluation-form"><div className="field-group"><label>Candidato / vaga</label><select name="candidatura_id" required defaultValue=""><option value="" disabled>Selecione</option>{(candidaturas||[]).map((x:any)=><option key={x.id} value={x.id}>{x.candidatos?.nome} — {x.vagas?.cargo}</option>)}</select></div><div className="field-group"><label>Nota técnica</label><select name="nota_tecnica" required defaultValue="5">{[5,4,3,2,1].map(n=><option key={n} value={n}>{'★'.repeat(n)} ({n})</option>)}</select></div><div className="competencias"><b>Competências</b><div className="check-grid">{comps.map(x=><label key={x}><input type="checkbox" name="competencias" value={x}/> {x}</label>)}</div></div><div className="field-group full"><label>Parecer final</label><textarea name="parecer" rows={4}/></div><div className="decision-row"><button className="button" name="decisao" value="aprovar">Aprovar</button><button className="danger-button" name="decisao" value="reprovar">Reprovar</button><button className="small-button" name="decisao" value="pendente">Salvar parecer</button></div></form></div><div className="card table-card"><h2 className="section-title">Histórico</h2><div className="table-wrap"><table><thead><tr><th>Candidato</th><th>Vaga</th><th>Nota</th><th>Operador</th><th>Decisão</th></tr></thead><tbody>{(rows||[]).map((r:any)=><tr key={r.id}><td>{r.candidaturas?.candidatos?.nome||'—'}</td><td>{r.candidaturas?.vagas?.cargo||'—'}</td><td>{'★'.repeat(r.nota_tecnica)}</td><td>{r.usuarios?.nome||'—'}</td><td><span className={'badge '+(r.decisao==='aprovar'?'green':r.decisao==='reprovar'?'red':'blue')}>{r.decisao}</span></td></tr>)}</tbody></table></div></div></section>}