import {createClient} from '@/lib/supabase/server';
export default async function Page(){const s=await createClient();const [{count:vagas},{count:candidatos},{count:contratados},{count:entrevistas},{data:fin}]=await Promise.all([s.from('vagas').select('*',{count:'exact',head:true}),s.from('candidatos').select('*',{count:'exact',head:true}),s.from('candidaturas').select('*',{count:'exact',head:true}).eq('status','contratado'),s.from('entrevistas').select('*',{count:'exact',head:true}).eq('status','realizada'),s.from('financeiro').select('valor,status')]);const receita=(fin||[]).filter((x:any)=>x.status==='pago').reduce((a:any,x:any)=>a+Number(x.valor||0),0);return <section><h1 className="page-title">Relatórios</h1><p className="muted">Indicadores consolidados da operação de RH.</p><div className="grid report-grid"><div className="card report-stat"><span>Vagas</span><strong>{vagas||0}</strong></div><div className="card report-stat"><span>Candidatos</span><strong>{candidatos||0}</strong></div><div className="card report-stat"><span>Entrevistas realizadas</span><strong>{entrevistas||0}</strong></div><div className="card report-stat"><span>Contratações</span><strong>{contratados||0}</strong></div></div><div className="card" style={{marginTop:16}}><h2 className="section-title">Resumo financeiro</h2><div className="report-money">{receita.toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}</div><div className="muted">Receita registrada como paga</div></div></section>}