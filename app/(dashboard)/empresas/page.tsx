import {createClient} from '@/lib/supabase/server';
export default async function Page(){const s=await createClient();const{data:rows}=await s.from('empresas').select('*').order('nome');return <section><h1 className="page-title">Empresas</h1><p className="muted">Clientes e parceiros atendidos pela AP Consultoria.</p><div className="card table-card"><div className="table-head"><h2 className="section-title" style={{margin:0}}>Empresas cadastradas</h2><span className="muted">{rows?.length||0} empresa(s)</span></div><div className="table-wrap"><table><thead><tr><th>Empresa</th><th>CNPJ</th><th>Contato</th><th>E-mail</th><th>Telefone</th><th>Status</th></tr></thead><tbody>{(rows||[]).map((r:any)=><tr key={r.id}><td><b>{r.nome}</b><div className="muted">{r.razao_social||''}</div></td><td>{r.cnpj||'—'}</td><td>{r.contato||'—'}</td><td>{r.email||'—'}</td><td>{r.telefone||'—'}</td><td><span className={'badge '+(r.ativo?'green':'gray')}>{r.ativo?'Ativa':'Inativa'}</span></td></tr>)}</tbody></table>{!rows?.length&&<div className="empty">Nenhuma empresa cadastrada.</div>}</div></div></section>}