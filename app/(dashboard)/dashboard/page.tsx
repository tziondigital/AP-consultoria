import {createClient} from '@/lib/supabase/server';import {Briefcase,CalendarDays,Users,UserCheck,Video,MoreVertical} from 'lucide-react';
const months=['Jan','Fev','Mar','Abr','Mai','Jun','Jul','Ago','Set','Out','Nov','Dez'];
function Kpi({label,value,Icon,note}:{label:string,value:any,Icon:any,note:string}){return <div className="mock-card mock-kpi"><div className="mock-icon"><Icon size={25}/></div><div><strong>{value}</strong><span>{label}</span><small>↑ {note}</small></div></div>}
export default async function Dashboard(){const s=await createClient();const now=new Date();const today=now.toISOString().slice(0,10);const [{data:profile},{count:vagas},{count:candidatos},{count:contratados},{data:rows},{data:agenda},{data:hires},{data:origens},{data:processos},{data:avaliacoes}]=await Promise.all([s.from('usuarios').select('nome').limit(1).maybeSingle(),s.from('vagas').select('*',{count:'exact',head:true}).eq('status','em_andamento'),s.from('candidatos').select('*',{count:'exact',head:true}),s.from('candidaturas').select('*',{count:'exact',head:true}).eq('status','contratado').gte('updated_at',new Date(now.getFullYear(),now.getMonth(),1).toISOString()),s.from('vagas').select('id,requisicao,cargo,status,status_legado,local,estado,created_at,empresas(nome),candidaturas(id,status)').eq('status','em_andamento').order('created_at',{ascending:false}).limit(5),s.from('entrevistas').select('id,data,status,link,candidatos(nome),vagas(cargo)').gte('data',today+'T00:00:00').lte('data',today+'T23:59:59').order('data').limit(6),s.from('candidaturas').select('updated_at').eq('status','contratado').gte('updated_at',new Date(now.getFullYear(),0,1).toISOString()),s.from('candidaturas').select('origem'),s.from('candidaturas').select('id,status,candidatos(nome,cargo),vagas(cargo)').order('inscrito_em',{ascending:false}).limit(14),s.from('avaliacoes').select('nota_tecnica,parecer,decisao,candidaturas(candidatos(nome,cargo))').order('created_at',{ascending:false}).limit(1)]);const bars=Array(12).fill(0);(hires||[]).forEach((h:any)=>bars[new Date(h.updated_at).getMonth()]++);const max=Math.max(...bars,1);const sources=(origens||[]).reduce((a:any,x:any)=>{const k=x.origem||'Outros';a[k]=(a[k]||0)+1;return a},{});const top=Object.entries(sources).sort((a:any,b:any)=>b[1]-a[1]).slice(0,5);const first=profile?.nome?.split(' ')[0]||'Ana';const av:any=avaliacoes?.[0];return <section className="mock-dashboard"><div className="mock-welcome"><div><h1>Olá, {first}!</h1><p>Aqui está o resumo da sua operação de recrutamento e seleção.</p></div><span>{new Intl.DateTimeFormat('pt-BR',{dateStyle:'full'}).format(now)}</span></div><div className="mock-kpis"><Kpi label="Vagas abertas" value={vagas||0} Icon={Briefcase} note="2 novas esta semana"/><Kpi label="Candidatos" value={candidatos||0} Icon={Users} note="novos no mês"/><Kpi label="Entrevistas hoje" value={agenda?.length||0} Icon={CalendarDays} note="confirmadas"/><Kpi label="Contratações no mês" value={contratados||0} Icon={UserCheck} note="resultado mensal"/></div><div className="mock-row mock-main-row"><div className="mock-card"><div className="mock-title"><b>Vagas em andamento</b><a href="/vagas">Ver todas →</a></div><table><thead><tr><th>Requisição</th><th>Cargo</th><th>Empresa</th><th>Local</th><th>Etapa</th><th>Prazo</th></tr></thead><tbody>{(rows||[]).map((r:any)=><tr key={r.id}><td>{r.requisicao||'—'}</td><td>{r.cargo}</td><td>{r.empresas?.nome||'—'}</td><td>{[r.local,r.estado].filter(Boolean).join('/')}</td><td><span className="pill blue">{r.status_legado||'Candidatos'}</span></td><td>{Math.max(0,Math.floor((Date.now()-new Date(r.created_at).getTime())/86400000))} dias</td></tr>)}</tbody></table></div><div className="mock-card"><div className="mock-title"><b>Agenda de hoje</b><a href="/entrevistas">Ver agenda →</a></div><div className="mock-agenda">{(agenda||[]).map((x:any)=><div key={x.id}><strong>{new Date(x.data).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}</strong><b>{x.candidatos?.nome}</b><small>{x.vagas?.cargo}</small><span className={'pill '+(x.status==='confirmada'?'green':'yellow')}>{x.status}</span><Video size={16}/><MoreVertical size={14}/></div>)}</div></div></div><div className="mock-row mock-charts"><div className="mock-card"><div className="mock-title"><b>Contratações por mês</b><span>{now.getFullYear()}</span></div><div className="mock-bars">{bars.slice(0,9).map((v,i)=><div key={i}><b>{v||''}</b><i style={{height:(16+(v/max)*82)+'px'}}/><small>{months[i]}</small></div>)}</div></div><div className="mock-card"><div className="mock-title"><b>Origem dos candidatos</b></div><div className="mock-origin"><div className="mock-donut"><b>{origens?.length||0}</b><span>candidatos</span></div><div>{top.map(([k,v]:any)=><p key={k}><span>{k}</span><b>{Math.round(v/Math.max(origens?.length||1,1)*100)}%</b></p>)}</div></div></div><div className="mock-brand"><div><b>Conectando<br/>talentos,<br/>gerando valor.</b></div></div></div><div className="mock-row mock-bottom"><div className="mock-card vacancy-preview"><div className="mock-title"><b>Ficha da Vaga</b><a href="/vagas">Editar vaga</a></div>{rows?.[0]?<><div className="tabs">Resumo　 Candidatos　 Entrevistas　 Avaliações　 Financeiro　 Histórico</div><h3>{rows[0].requisicao||'—'} - {rows[0].cargo}</h3><div className="facts"><span>Empresa<b>{rows[0].empresas?.nome||'—'}</b></span><span>Local<b>{[rows[0].local,rows[0].estado].filter(Boolean).join('/')}</b></span><span>Candidatos<b>{rows[0].candidaturas?.length||0}</b></span></div></>:null}</div><div className="mock-card"><div className="mock-title"><b>Kanban do Processo Seletivo</b></div><div className="mini-kanban">{[['novo','Recebidos'],['entrevista','Em Entrevista'],['aprovado','Finalistas'],['contratado','Contratados']].map(([st,l])=><div key={st}><b>{l} ({(processos||[]).filter((x:any)=>x.status===st||(st==='novo'&&x.status==='triagem')).length})</b>{(processos||[]).filter((x:any)=>x.status===st||(st==='novo'&&x.status==='triagem')).slice(0,3).map((x:any)=><span key={x.id}><strong>{x.candidatos?.nome}</strong><small>{x.vagas?.cargo}</small></span>)}</div>)}</div></div><div className="mock-card mock-eval"><div className="mock-title"><b>Avaliação de Candidato</b></div><h3>{av?.candidaturas?.candidatos?.nome||'Selecione um candidato'}</h3><small>{av?.candidaturas?.candidatos?.cargo||''}</small><p><b>Nota técnica　</b><em>{'★'.repeat(av?.nota_tecnica||4)}☆</em></p><b>Competências</b><p className="checks">☑ Comunicação<br/>☑ Trabalho em equipe<br/>☑ Organização</p><label>Parecer final</label><div className="fake-textarea">{av?.parecer||'Candidato com bom perfil, demonstrou interesse e aderência à vaga.'}</div><div className="eval-actions"><button>Reprovar</button><button>Aprovar</button></div></div></div></section>}