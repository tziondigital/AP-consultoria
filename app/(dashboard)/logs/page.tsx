import {createClient} from "@/lib/supabase/server";
export default async function Page(){const s=await createClient();const{data:rows}=await s.from("audit_logs").select("*").order("created_at",{ascending:false}).limit(100);return <section className="module-page"><h1 className="page-title">Logs de auditoria</h1><p className="muted">Rastreabilidade das alterações realizadas no sistema.</p><div className="card table-card"><div className="table-wrap"><table><thead><tr><th>Data</th><th>Ação</th><th>Tabela</th><th>Registro</th><th>Usuário</th></tr></thead><tbody>{(rows||[]).map((r:any)=><tr key={r.id}><td>{new Date(r.created_at).toLocaleString("pt-BR")}</td><td>{String(r.acao||"").replaceAll("_"," ")}</td><td>{r.tabela}</td><td>{r.registro_id||"—"}</td><td>{r.usuario_id||"Sistema"}</td></tr>)}</tbody></table></div></div></section>}