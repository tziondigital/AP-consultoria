"use client";import {useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function LoginPage(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const s=createClient();const{error}=await s.auth.signInWithPassword({email,password});if(error)return setMsg(error.message);location.href='/dashboard'}return <main className="min-h-screen grid place-items-center p-6 bg-[#0B1F3A]"><form onSubmit={submit} className="ap-card w-full max-w-md p-8 space-y-5"><h1 className="text-3xl font-extrabold text-[#0B1F3A]">Portal de Recrutamento</h1><input className="ap-input" type="email" placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="ap-input" type="password" placeholder="Senha" value={password} onChange={e=>setPassword(e.target.value)} required/>{msg&&<p className="text-sm text-red-600">{msg}</p>}<button className="ap-button ap-primary w-full">Entrar</button></form></main>}