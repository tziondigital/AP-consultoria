'use client';import {useState} from 'react';import {Plus} from 'lucide-react';
export function UserInviteForm(){const[state,setState]=useState('');async function submit(f:FormData){setState('Enviando...');const res=await fetch('/api/usuarios/invite',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});const json=await res.json();setState(res.ok?'Convite enviado com sucesso.':json.error||'Falha ao enviar convite.');if(res.ok)location.reload()}return <details className="action-popover"><summary><Plus size={16}/> Novo usuário</summary><form className="popover-form" action={submit}><input name="nome" placeholder="Nome completo" required/><input name="email" type="email" placeholder="E-mail" required/><input name="cargo" placeholder="Cargo"/><input name="telefone" placeholder="Telefone"/><select name="perfil" defaultValue="cliente"><option value="admin">Administrador</option><option value="operador">Recrutador</option><option value="cliente">Cliente</option></select><button className="primary-button">Enviar convite</button>{state&&<small>{state}</small>}</form></details>}
